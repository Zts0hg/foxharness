# Tasks: Agent Architecture Decoupling

**Input**: `requirements.md`, `spec.md`, and approved `plan.md` in this feature directory
**Prerequisites**: `review-spec.md` and `review-plan.md` both PASS
**Workflow**: Sequential execution on the current integration branch; strict Red-Green-Refactor for new production behavior and defect corrections

## Task Format and Gate Rules

- Every task has one verifiable outcome, exact or bounded paths, dependencies, and upstream coverage.
- A production task records its Red command and expected failure, minimal Green result, Refactor result, and final Green commands.
- A mechanical move uses existing green characterization and does not create an artificial Red.
- Every dependency-changing task updates `docs/package-dependencies.md`, its Mermaid graph, the architecture test, and the decreasing allowlist in the same commit.
- No production architecture task (`T047` onward) may start before `T046` freezes `B00`.
- If a `DV-*` task proves a defect, execution stops for a separately confirmed correction requirement and independent TDD defect commit. The corrected implementation is rerun before `T045` and `T046`.
- Each `Mxx` production boundary is one independently green, reviewable, bisectable, and revertible commit unless the confirmed narrow mechanical-combination exception applies.

## Phase 0A: Characterization Infrastructure

- [x] **T001** Create `.codexspec/specs/2026-0808-17255q-agent-architecture-decoupling/characterization-trace.md` with one row for every required `RT`, `ST`, `TL`, `CX`, `PL`, `RS`, `PF-*`, `UI-*`, `EV-BEN`, `IA-CHD`, `CP-AUT`, and `DV-*` ID, including test, fixture/expected outcome, command, status, and source commit fields. Verify the ID set exactly matches `requirements.md`. Dependencies: none. Covers: NFR-005, NFR-007, NFR-010; Plan: Phase 0 trace authority.
- [x] **T002** Create the initial authoritative `docs/package-dependencies.md` with the target Mermaid DAG, responsibilities, forbidden edges, composition exceptions, interaction/observation flow, and injection points; implement the standard-library Go import parser and exact current-violation allowlist in `internal/architecturetest/imports_test.go` and `internal/architecturetest/allowlist.json`; verify new/broadened violations fail. Dependencies: T001. Covers: REQ-015, NFR-003, NFR-006, NFR-012; Plan: PLD-002 and Phase 0 architecture baseline.
- [x] **T003** Implement immutable fixture manifest types, SHA-256 verification, copy-to-temp helpers, deterministic clocks/IDs, and self-tests in `internal/testsupport/entryfixture/` and `testdata/characterization/v1/manifest.json`. Dependencies: T001. Covers: NFR-006, NFR-009; Plan: PLD-006 and Phase 0 fixtures.
- [x] **T004** Define the implementation-neutral runtime scenario DSL and adapter contract in `internal/testsupport/runtimecontract/`, with self-tests proving ordered fact, outcome, artifact, and fixture assertions. Dependencies: T001, T003. Covers: NFR-007, NFR-008; Plan: PLD-001 and Shared Characterization Harness.
- [x] **T005** Add a current-production test adapter in the closest existing engine/app test packages and prove one tool-free contract scenario can execute without exporting a production compatibility API. Dependencies: T004. Covers: NFR-005, NFR-008; Plan: Phase 0 current adapter.
- [x] **T006** Add deterministic scripted model, streaming, tool, interaction, filesystem, process, transport, messenger, local-Git, and fake-GitHub collaborators under `internal/testsupport/`; prove they use no external network, credentials, ambient HOME, fixed ports, uncontrolled clocks, or sleep-based concurrency. Dependencies: T003, T004. Covers: NFR-006, NFR-007, NFR-010; Plan: Hermetic collaborators.

**Checkpoint P0A**: Infrastructure tests pass and T001 rows identify every mandatory scenario, but no row may be marked baseline-passing without its executable test.

## Phase 0B: Residual Defect Verification and Fixture Authority

- [x] **T040** Execute comprehensive hermetic proofs for `DV-FEI-001..010` in Feishu/cmd tests, record each defect/not-defect result and evidence, and stop for separate requirements on any proven defect. Dependencies: T006. Covers: NFR-005, NFR-006, NFR-010; Plan: Feishu defect gate. **Correction stop cleared:** T074-T083 corrected all ten proven defects through independent Green commits; T041 may proceed after the complete post-correction gate.
- [x] **T074 (`D-FEI-001`)** TDD the authenticated bounded approval HTTP callback and deterministic status mapping; update `DV-FEI-001` only after Green. Dependencies: T040, DEC-042 confirmation. Covers: NFR-006, NFR-010.
- [x] **T075 (`D-FEI-002`)** TDD the atomic durable at-most-once Feishu message acceptance store, duplicate acknowledgement, restart behavior, and live rollback; update `DV-FEI-002` only after Green. Dependencies: T074. Covers: NFR-006, NFR-010.
- [x] **T076 (`D-FEI-003`)** TDD missing/blank sender rejection before reservation, lookup, and enqueue while preserving successful webhook acknowledgement; update `DV-FEI-003` only after Green. Dependencies: T075. Covers: NFR-006, NFR-010.
- [x] **T077 (`D-FEI-004`)** TDD acceptance-scoped timeout and cancellation-aware same-session waiting so expired work never starts; update `DV-FEI-004` only after Green. Dependencies: T076. Covers: NFR-006, NFR-010.
- [x] **T078 (`D-FEI-005`)** TDD per-session FIFO scheduling and global-permit fairness; update `DV-FEI-005` only after Green. Dependencies: T077. Covers: NFR-006, NFR-010.
- [x] **T079 (`D-FEI-006`)** TDD Runner drain/cancel behavior and signal-aware HTTP/task shutdown in `cmd/feishu`; update `DV-FEI-006` only after Green. Dependencies: T078. Covers: NFR-006, NFR-010.
- [x] **T080 (`D-FEI-007`)** TDD non-blocking exactly-once approval resolution and terminal cleanup; update `DV-FEI-007` only after Green. Dependencies: T079. Covers: NFR-006, NFR-010.
- [x] **T081 (`D-FEI-008`)** TDD frozen selected-model propagation to engine and compactor; update `DV-FEI-008` only after Green. Dependencies: T080. Covers: NFR-006, NFR-010.
- [x] **T082 (`D-FEI-009`)** TDD correlated panic terminal outcomes, bounded failure delivery, and cleanup; update `DV-FEI-009` only after Green. Dependencies: T081. Covers: NFR-006, NFR-010.
- [x] **T083 (`D-FEI-010`)** TDD typed delivery failures, bounded transport text, and production observation; update `DV-FEI-010` only after Green and clear the T040 correction stop. Dependencies: T082. Covers: NFR-006, NFR-010.
- [x] **T041** Execute proofs for `DV-AOP-001..006` plus reuse corrected `DV-FEI-001` and `DV-FEI-007` behavior without a second approval protocol; record each result and stop on proven defects. Dependencies: T006, T040, T074-T083. Covers: NFR-005, NFR-006, NFR-010; Plan: AgentOps defect gate. **Correction stop cleared:** T084-T089 corrected all six proven defects through independent Green commits; shared authenticated exactly-once approval reuse remains verified and T042 may proceed.
- [x] **T084 (`D-AOP-001`)** TDD composition of the shared durable Gateway `DeliveryStore` as AgentOps' sole task-acceptance authority, remove the process-local acceptance `Deduper`, and prove duplicate acknowledgement, rollback, concurrency, and restart behavior; update `DV-AOP-001` only after Green. Dependencies: T041, DEC-043. Covers: NFR-006, NFR-010.
- [x] **T085 (`D-AOP-002`)** TDD coordinated HTTP, bridge, two-channel, queued-task, and active-task shutdown with producer-safe close order, ordinary drain, cancellation outcomes, and one bounded process wait; update `DV-AOP-002` only after Green. Dependencies: T084. Covers: NFR-006, NFR-010.
- [x] **T086 (`D-AOP-003`)** TDD one Runner-owned typed terminal transition for success, ordinary failure, timeout, cancellation, and panic, including fresh bounded terminal delivery contexts and cleanup; update `DV-AOP-003` only after Green. Dependencies: T085. Covers: NFR-006, NFR-010.
- [x] **T087 (`D-AOP-004`)** TDD one immutable provider/model snapshot shared by engine, compactor, telemetry-relevant metadata, and child inheritance for each task; update `DV-AOP-004` only after Green. Dependencies: T086. Covers: NFR-006, NFR-010.
- [x] **T088 (`D-AOP-005`)** TDD typed delivery-failure observation, Runner-boundary text limits, separate task and delivery outcomes, and non-recursive terminal-send failure handling; update `DV-AOP-005` only after Green. Dependencies: T087. Covers: NFR-006, NFR-010.
- [x] **T089 (`D-AOP-006`)** TDD rooted regular-file access for `log_search` with traversal, separator, symlink, final-target, cancellation, 200-line, and one-MiB-line assertions; update `DV-AOP-006` only after Green and clear the T041 correction stop. Dependencies: T088. Covers: NFR-006, NFR-010.
- [x] **T042** Execute proofs for `DV-BEN-001..007`, distinguishing runtime, evaluation, infrastructure, cancellation, timeout, path, process-tree, output-bound, provenance, and status outcomes; record and stop on defects. Dependencies: T006, T084-T089. Covers: REQ-013, NFR-005, NFR-006, NFR-010; Plan: Benchmark defect gate. **Correction stop cleared:** T090-T096 corrected all seven proven defects through independent Green commits; the complete repository gate passes and T043 may proceed.
- [x] **T090 (`D-BEN-001`)** TDD one case-owned 600-second default deadline covering setup, runtime, validation, and reporting inputs, with distinct cancellation/timeout outcomes and synthetic ordered records for unstarted validations; prove an expired case context is not the cleanup authority and update `DV-BEN-001` only after Green. Dependencies: T042, DEC-044. Covers: REQ-013, NFR-006, NFR-010.
- [x] **T091 (`D-BEN-002`)** TDD typed repeat status, separate runtime/evaluation/infrastructure evidence, partial reporting, and process exit codes 0/1/2; update `DV-BEN-002` only after Green. Dependencies: T090. Covers: REQ-013, NFR-006, NFR-010.
- [x] **T092 (`D-BEN-003`)** TDD one immutable typed current `BenchmarkRuntimeSpec` used for construction and derived machine/human fidelity without introducing the future general profile layer; update `DV-BEN-003` only after Green. Dependencies: T091. Covers: REQ-013, NFR-006, NFR-010.
- [x] **T093 (`D-BEN-004`)** TDD rooted fixture copy and `file_contains`, source-symlink and unsupported-type rejection, source immutability, success retention, failed-workspace cleanup under one fresh 30-second context, and typed cleanup failure/timeout evidence; update `DV-BEN-004` only after Green. Dependencies: T092. Covers: REQ-013, NFR-006, NFR-010.
- [x] **T094 (`D-BEN-005`)** TDD strict repeat, case, turn-budget, YAML, fixture-resolution, and validation-field domains with deterministic error precedence; update `DV-BEN-005` only after Green. Dependencies: T093. Covers: REQ-013, NFR-006, NFR-010.
- [x] **T095 (`D-BEN-006`)** TDD isolated validator process trees, independent one-MiB stdout/stderr bounds, overflow status, TERM-to-KILL cleanup, reaping, and ordered synthetic post-cancellation results; update `DV-BEN-006` only after Green. Dependencies: T094. Covers: REQ-013, NFR-006, NFR-010.
- [x] **T096 (`D-BEN-007`)** TDD schema version, one-based repeat, run/case-definition/fixture identities, terminal cause, provider/model, deadline, runtime-spec provenance, normalized golden behavior, and corrected-schema stability; update `DV-BEN-007` only after Green and clear the T042 correction stop. Dependencies: T095. Covers: REQ-013, NFR-006, NFR-010.
- [x] **T043** Execute proofs for `DV-CHD-001..006`, separating invocation-adapter and runtime-lifecycle behavior and preserving the one-level ceiling; record and stop on defects. Dependencies: T006, T090-T096. Covers: REQ-012, NFR-005, NFR-006, NFR-010; Plan: Child defect gate. **Correction stop cleared:** T097-T102 corrected all six proven defects through independent Red-Green-Refactor commits; the complete ChildRun and repository gates pass and T044 may proceed.
- [x] **T097 (`D-CHD-002`)** TDD one immutable child execution snapshot shared by provider invocation, model identity and context window, proactive and reactive compaction, telemetry, and persisted compact state, including one explicit observable unknown-model fallback; update `DV-CHD-002` only after Green. Dependencies: T043, DEC-045. Covers: REQ-012, NFR-006, NFR-010.
- [x] **T098 (`D-CHD-005`)** TDD fork-agent normalization and resolution before session creation, default `general-purpose`, explicit unknown-agent rejection, persona and tool narrowing, frozen-ceiling enforcement, and agent identity propagation through run input, lineage, outcome, and telemetry; update `DV-CHD-005` only after Green. Dependencies: T097. Covers: REQ-012, NFR-006, NFR-010.
- [x] **T099 (`D-CHD-003`)** TDD one immutable effective child tool snapshot derived from profile, access mode, selected-agent narrowing, and caller allowlist and shared by definitions, execution, aliases, permission assessment, parallel-safety lookup, and prompt fragments; remove unsupported write, TODO, skill, interaction, and delegation claims and update `DV-CHD-003` only after Green. Dependencies: T098. Covers: REQ-012, NFR-006, NFR-010.
- [x] **T100 (`D-CHD-001`)** TDD compatible model-visible read-only Bash with fail-closed shell parsing, rejection of mutation/background/detach/dynamic forms, execution-layer read-only filesystem and network containment, isolated environment, no permission expansion, and unrestricted-fallback rejection; update `DV-CHD-001` only after Green. Dependencies: T099. Covers: REQ-012, NFR-006, NFR-010.
- [x] **T101 (`D-CHD-006`)** TDD exactly one typed correlated child outcome for success, failure, cancellation, exhaustion, rejection, and start failure; preserve invocation, lineage, session/run identity, error classification, and only the latest committed assistant partial report through both delegate and fork adapters without presenting partial content as success; update `DV-CHD-006` only after Green. Dependencies: T100. Covers: REQ-012, NFR-006, NFR-010.
- [x] **T102 (`D-CHD-004`)** TDD synchronous child shell supervision that rejects background/detach semantics, registers processes before execution, contains and reaps complete process trees, cancels pending permissions, drains every terminal path before publishing its typed outcome, and represents cleanup failure explicitly; update `DV-CHD-004` only after Green and clear the T043 correction stop after the complete post-correction gate. Dependencies: T101. Covers: REQ-012, NFR-006, NFR-010.
- [x] **T044** Execute proofs for `DV-AUT-001..010` across ledger durability, stage validity, identity, materialization, paths, processes, publishing, extraction, concurrency configuration, and partial outcomes; record and stop on defects. Dependencies: T006, T097-T102. Covers: REQ-011, NFR-005, NFR-006, NFR-010; Plan: Autodev defect gate. **Correction stop cleared:** T103-T112 corrected all ten proven defects through independent Red-Green-Refactor commits; the complete Autodev and repository gates pass and T045 may proceed.
- [x] **T103 (`D-AUT-001`)** TDD transactional ledger mutation with candidate-state commit, atomic durable flush, stable stage or publication attempt identity, fail-stop behavior before every dependent side effect, retained worktrees, and deterministic restart reconciliation; update `DV-AUT-001` only after Green. Dependencies: T044, DEC-046. Covers: REQ-011, NFR-006, NFR-010. **Red**: the focused test failed because transactional `Ledger.Commit` did not exist, while the retained T044 proof demonstrated warning-and-continue behavior. **Green**: candidate state commits only after atomic file flush and rename, every SDD and publication intent is durable before execution, issue and PR bindings precede reports and dependent work, every injected persistence failure stops and retains the worktree, and focused, race, vet, and complete repository gates pass.
- [x] **T104 (`D-AUT-002`)** TDD schema-versioned typed stage and `pending`/`running`/`verified` transitions, explicit known-state migration, fail-closed invalid or future state, and verify-first resume without pipeline bypass; update `DV-AUT-002` only after Green. Dependencies: T103. Covers: REQ-011, NFR-006, NFR-010. **Red**: focused tests failed to compile because production exposed no typed stage/state vocabulary or invalid-ledger error, while the retained T044 proof demonstrated unknown-stage pipeline bypass. **Green**: the versioned schema validates every legal lifecycle combination before external work or persistence, known older records migrate explicitly, unknown/future/inconsistent state fails closed, running SDD and remote stages verify ground truth before core execution, verified stages advance monotonically, every transition is durable, and focused, package, race, vet, and complete repository gates pass.
- [x] **T105 (`D-AUT-003`)** TDD immutable item and optional source identity, one-time unambiguous legacy binding, duplicate and ambiguous reconciliation conflicts, orphaned or blocked missing items, current pending order, advisory-status isolation, and frozen in-progress requirement revisions; update `DV-AUT-003` only after Green. Dependencies: T104. Covers: REQ-011, NFR-006, NFR-010. **Red**: corrected tests failed to compile because items and ledger records exposed no source/item identity, reconciliation result, source state, or frozen revision; successive Reds proved active legacy binding and workflow callback identity rewrites remained unsafe. **Green**: schema v2 persists immutable `ItemID`, optional normalized source ID, full requirement text/bytes/hash, source order/state, freeze state, and deferred one-time legacy binding; duplicate, rename, replacement, missing-active, and active-edit ambiguity fails closed without worktree/core/remote execution, while pending source updates and order remain current and advisory status remains ignored. Focused, package, race, vet, and complete repository gates pass.
- [x] **T106 (`D-AUT-004`)** TDD exact UTF-8 backlog description preservation under a 64-MiB whole-file ceiling, multiline and fenced Markdown, Unicode, deterministic empty fallback, full requirements materialization, and item, byte-length, and SHA-256 traceability without authoritative truncation; update `DV-AUT-004` only after Green. Dependencies: T105. Covers: REQ-011, NFR-006, NFR-010. **Red**: corrected tests proved multiline/long content was collapsed or truncated, Scanner rejected valid large lines, fenced metadata was misparsed, and stale truncated artifacts bypassed rematerialization. **Green**: bounded whole-file UTF-8 parsing preserves normalized multiline/fenced content, materialization includes exact authority plus identity/bytes/hash and rebuilds stale artifacts, and focused, package, race, vet, and complete repository gates pass.
- [x] **T107 (`D-AUT-005`)** TDD one `os.Root`-backed feature-workspace authority for every artifact consumer, strict normalized feature-directory validation, no-follow component and final-target checks, symlink and non-regular rejection, and fail-closed legacy ledger handling; update `DV-AUT-005` only after Green. Dependencies: T106. Covers: REQ-011, NFR-006, NFR-010. **Red**: corrected tests proved absolute, traversal, malformed, directory-symlink, final-symlink, and non-regular paths were accepted; legacy persisted escapes loaded successfully; and core construction preceded workspace rejection. **Green**: one rooted authority validates the exact logical path, anchors all artifact I/O with `os.Root`, checks every directory component and final target without following links, atomically replaces only regular artifacts, preflights resume targets before lazy core construction, and fails closed on illegal current or legacy ledger values. Focused, package, race, vet, and complete repository gates pass.
- [x] **T108 (`D-AUT-006`)** TDD structured independent one-MiB stdout and stderr capture, typed overflow, strict JSON-query overflow failure, query, gate, and stage-attempt deadlines, registered process-tree supervision, bounded TERM-to-KILL reaping, post-cancel suppression, and SIGINT/SIGTERM exit mapping; update `DV-AUT-006` only after Green. Dependencies: T107. Covers: REQ-011, NFR-006, NFR-010. **Red**: corrected tests failed to compile because runners exposed only unbounded combined strings and no overflow type; behavior proofs then showed descendants survived cancellation, canceled gates all started, core attempts had no deadline, and canceled publishing persisted a new remote intent. **Green**: structured runners independently bound both streams, strict consumers reject typed overflow, gates preserve exit status with explicit truncation, command classes and stage/core attempts receive caller-aware deadlines, supervised process groups use bounded TERM-to-KILL reaping and final cleanup, canceled workflows suppress later work, and CLI signals cancel Autodev with 130/143 status. Focused, package, race, vet, cross-platform compile, and complete repository gates pass.
- [x] **T109 (`D-AUT-007`)** TDD stable item markers in issue bodies, recorded-number revalidation, complete marker pagination, zero/one/multiple-match behavior, closed-issue reuse, durable binding before PR work, and outbox event identity with idempotent at-least-once reporting; update `DV-AUT-007` only after Green. Dependencies: T108. Covers: REQ-011, NFR-006, NFR-010. **Red**: corrected tests failed to compile because issue verification exposed neither immutable marker identity, conflict-capable verification, durable remote events, nor outbox state; successive Reds proved ledger copies leaked mutable outbox state and TUI/terminal delivery could not reject failed consumption. **Green**: exact `ItemID` markers drive recorded-number reads and complete paginated search with zero/one/multiple semantics independent of mutable title or closed state; issue binding and one pending stable `EventID` commit atomically before reporting and PR work; pending events replay before later work, adapters acknowledge only successful idempotent consumption, and ledger validation prevents identity/history rewrites. Focused, package, TUI, race, vet, and complete repository gates pass.
- [x] **T110 (`D-AUT-008`)** TDD item-scoped post-run `Drain`/`Close` ownership, per-run and final extraction joins, deterministic next-run and next-item memory visibility, cancellation and fresh bounded lifecycle cleanup, warning-only extraction failures, and retained worktree on undrained work; update `DV-AUT-008` only after Green. Dependencies: T109. Covers: REQ-011, NFR-006, NFR-010. **Red**: corrected tests failed because `CoreRunner` exposed no awaitable lifecycle or typed lifecycle failure, and the app adapter could not satisfy the required `Drain`/`Close` contract. **Green**: StageMachine drains after every core attempt before verification, retry, or another run; orchestrator closes once under a fresh bounded context before done, next item, return, or worktree removal and retains the worktree on failure. Autodev alone explicitly binds AgentRunner extraction to its item context, while ordinary profiles preserve background extraction. Parent cancellation cancels real extraction, drain/close join it, provider errors and panics remain warning-only, and focused, package, architecture, race, vet, and complete repository gates pass.
- [x] **T111 (`D-AUT-009`)** TDD the exact concurrency domain of omitted or case-sensitive `serial`, reject whitespace, case, numeric, parallel, and future values before preconditions, and prove every accepted configuration remains serial without warning fallback; update `DV-AUT-009` only after Green. Dependencies: T110. Covers: REQ-011, NFR-006, NFR-010. **Red**: the retained proof accepted every scalar string silently, while corrected tests could not compile without typed invalid-concurrency evidence. **Green**: the YAML wire form preserves field presence, node kind, and tag; omission and exact string `serial` alone resolve successfully, while explicit empty/null, whitespace, case variants, numeric, sequence, parallel, and future values return `InvalidConcurrencyError`. Orchestrator revalidates resolved config before ledger or preconditions, no warning fallback exists, accepted execution remains serial, and focused, package, race, vet, and complete repository gates pass.
- [x] **T112 (`D-AUT-010`)** TDD one typed correlated core outcome for success, failure, cancellation, exhaustion, and start failure; preserve session, run, committed partial, cause, retry class, and lifecycle evidence; verify every started attempt, persist retry correlation, enforce fresh-runner and cancellation policy, and never present partial evidence as success; update `DV-AUT-010` only after Green and clear the T044 correction stop after the complete Autodev and repository gates. Dependencies: T111. Covers: REQ-011, NFR-006, NFR-010. **Red**: corrected tests failed to compile because `CoreRunner.Run` exposed only an independently nullable result/error pair and no typed outcome, retry class, lifecycle evidence, attempt identity, durable retry record, partial-only review DTO, or replaceable item owner; the production closure then failed the fresh-runner test. **Green**: one validated `CoreOutcome` preserves terminal status, cause unwrapping, session/run IDs, committed assistant partial, correlation, retry class, and lifecycle evidence. Every started attempt drains then verifies, cancellation reconciles under a fresh bounded context and still stops after durable verification, retries persist running/terminal evidence first, persistence-class failures replace and close the old runner, partial evidence is explicitly non-final, schema v3 preserves v2 compatibility, and focused, package, race, vet, and complete repository gates pass. T044 correction stop is cleared.
- [x] **T045** After every proven defect has a separately approved and green correction commit, generate the immutable `testdata/characterization/v1` persisted-session and exact-output fixtures once from the corrected source commit, complete manifest semantics/hashes, and pass fixture read/copy/integrity tests. Dependencies: T040-T044, T074-T112. Covers: REQ-006, REQ-014, NFR-001, NFR-006, NFR-009, NFR-010; Plan: Baseline fixture authority. **Completed:** froze 22 persisted-session and exact-output files from corrected source `ee649228970ed08cbf567df4f6ca576560323585`; the manifest records per-file source, semantics, explicit normalization, and SHA-256, rejects incomplete or mixed authorities, and verifies the complete directory. Copy-isolation and current production session, compaction, checkpoint, and memory-history readers pass against copied fixtures; the benchmark golden now uses the shared authority. Full repository tests, affected race tests, and `go vet ./...` pass.

**Checkpoint P0B**: Every `DV-*` item has a result, all required corrections are green, and immutable fixture bytes identify the corrected source commit.

## Phase 0C: Shared Runtime Characterization

- [x] **T010** Implement and pass `RT-001..007` through the current adapter in `internal/testsupport/runtimecontract/` plus existing engine/app test adapters; update all seven trace rows. Dependencies: T005, T006, T045. Covers: REQ-004, REQ-005, NFR-005, NFR-007, NFR-008; Plan: Shared runtime turns. **Completed:** Red first failed on missing request/metric/nil-response DSL fields and then on the tool-free adapter boundary; Green added only test-support observations and a current-production adapter for the complete turn catalog. Tool-free, correlated tool, mixed text/tool, thinking/action, unlimited and bounded turns, immutable invocation snapshots, empty/nil responses, and provider errors now pass with exact facts, outcomes, persisted records, and normalized metrics. Affected package and race gates pass.
- [x] **T011** Implement and pass `ST-001..006`, including deterministic pre-delta fallback, post-delta failure, disabled-stream state, and cross-run cleanup; update trace rows. Dependencies: T010. Covers: REQ-004, REQ-009, NFR-005, NFR-007; Plan: Shared streaming. **Completed:** Red failed on missing transport/error-kind DSL fields and streaming adapter support. Green added test-only streaming transport observation and a delta-capable reporter while preserving provider attempts separately from logical turn count. Successful deltas, unsupported/empty/retryable pre-delta fallbacks, post-delta failure ordering, same-run stream disabling, and cross-run cleanup pass against the corrected current engine.
- [x] **T012** Implement and pass `TL-001..008` with synchronization barriers, immutable tool snapshots, ordering, aliases, errors, large artifacts, and cancellation; update trace rows. Dependencies: T010. Covers: REQ-005, REQ-014, NFR-005, NFR-007; Plan: Shared tool lifecycle. **Completed:** Red proved the scripted current-production tool accepted mismatched arguments and therefore could not characterize invalid-input behavior; Green added test-only invocation validation. Exact successful lifecycle, four failure classes with continuation, alias capability agreement, 60KB artifact/model/reporter/persistence representations, channel-barrier parallel and exclusive scheduling, cancellation terminal states, and next-turn injection ordering now pass. Affected package and race gates, full repository tests, and `go vet ./...` pass.
- [x] **T113 (`D-CX-007`)** TDD rewind-to-compaction consistency so truncating before or within compact coverage invalidates the future-bearing summary before raw-history truncation, while truncating after coverage preserves the valid summary; verify frozen fixture projections and update `CX-007` only with T013's complete context gate. Dependencies: T012, T045. Covers: REQ-006, NFR-006, NFR-009. **Completed:** Red against three independent copies of the frozen context-lifecycle fixture proved coverage-before and coverage-within rewinds retained the seq-4 summary after deleting raw future messages. Green invalidates compact state when `rewindSeq <= CoveredUntilSeq`, preserves it after coverage, and leaves a fail-safe expanded raw-history projection if later truncation fails. Focused app/session/TUI, race, full repository, and vet gates pass.
- [x] **T114 (`D-CHD-007`)** TDD the already confirmed ChildRun admission immutability and hard 200-turn ceiling discovered while expanding T030: copy caller-owned allowlists before startup can block, allow only positive turn-budget narrowing, and prevent zero, negative, or over-ceiling values from weakening or expanding the profile. Dependencies: T030 characterization start, DEC-045. Covers: REQ-012, NFR-006, NFR-010. **Completed:** a session-creation barrier proved an active caller mutation could replace `[read_file]` with `[write_file]` before registry resolution, while the turn table proved `WithMaxTurns` accepted 201, zero, and negative values directly. Green snapshots the allowlist at Run admission and resolves only `1..199` as narrowing; all other values retain the 200 ceiling.
- [x] **T115 (`D-CHD-008`)** TDD the already confirmed runtime depth-one gate discovered while expanding T030 so explicit direct internal depth-two, fork, alias, or future adapter requests reject before agent resolution, session/capacity allocation, permission, supervisor, or engine work while existing zero-value callers normalize compatibly to depth one. Dependencies: T114, DEC-007, PF-CHD-011. Covers: REQ-012, NFR-006, NFR-010. **Completed:** compile Red proved `Request` had no depth contract and one-level behavior relied only on omitted delegation tools. Green adds an explicit depth value, normalizes existing zero-value requests to one, rejects every other depth with a typed correlated outcome before session creation, and records the resolved/rejected depth in the outcome.
- [x] **T116 (`D-CHD-009`)** TDD complete ChildRun runtime and permission-evidence lineage discovered while expanding T030: freeze parent run and delegation identities, persist them with the fresh child session, retain them in every typed terminal outcome, and correlate each child permission review with parent/child session and run, delegation, and exact child tool-call identity. Dependencies: T115, PF-CHD-002, PF-CHD-014. Covers: REQ-012, REQ-014, NFR-006, NFR-010. **Completed:** compile Red proved `Request`, `Result`, persisted `Session`, and `Evidence` had no parent-run, delegation, or structured correlation contract. Green adds value-owned lineage fields, injects the established child run before tool review, and binds the exact registry call ID without changing approval classification or grant semantics. T031 remains responsible for supplying live parent adapter lineage.
- [x] **T117 (`D-CHD-010`)** TDD live parent run and exact tool-call lineage across the `delegate_task` and fork-skill invocation adapters discovered while expanding T031. The current engine MUST attach session/run identity after run creation, the registry MUST add an isolated exact call ID for each tool execution, and both adapters MUST pass those values plus explicit depth one into the existing ChildRun request without changing schemas, output, or capability policy. Dependencies: T116, T031 characterization start, IA-CHD-004, IA-CHD-005. Covers: REQ-011, REQ-012, REQ-014, NFR-006, NFR-010. **Completed:** end-to-end Reds proved both adapters persisted empty parent run and generated a replacement delegation identity. Green adds a narrow context-carried invocation value, proves parallel call isolation under race, and preserves the live values through both child-session paths.
- [x] **T118 (`D-CHD-011`)** TDD fail-closed `delegate_task` execution when the Manager or nested child permission coordinator is unavailable, preserving the already human-only assessment and rejecting before session, provider, tool, or process work. Direct trusted ChildRun Manager calls and user-invoked fork commands remain outside this model-facing adapter gate; model-invoked fork skills retain their existing nested-coordination assessment. Dependencies: T117, IA-CHD-002, IA-CHD-003. Covers: REQ-011, REQ-012, NFR-006, NFR-010. **Completed:** Red proved assessment/execution disagreement and reached a panic-capable child startup path without a coordinator. Green returns one stable adapter error before child creation and updates successful adapter tests to provide an explicit hermetic coordinator.
- [x] **T013** Implement and pass `CX-001..008` against fresh/resumed/compacted/rewound fixture copies and exact model-visible projections; update trace rows. Dependencies: T003, T010, T012. Covers: REQ-006, REQ-007, NFR-005, NFR-007, NFR-009; Plan: Shared context lifecycle. **Completed:** Exact fresh, uncompacted, resumed-fixture, initial-compacted/reopened, pre-turn automatic, reactive retry, failure/breaker/reset, manual continuation/reopen/repeat, rewind, and tool-overhead/blocking projections pass. D-CX-007 was first proved and corrected in independent commit `2b4aa9b`; all other catalog work is characterization-only. Affected package and race gates, full repository tests, and `go vet ./...` pass.
- [x] **T014** Implement and pass `PL-001..005` for recovery, reminder, completion, TODO, cooldown, suppression, and ordering; update trace rows. Dependencies: T010, T012. Covers: REQ-005, NFR-005, NFR-007; Plan: Shared policies. **Completed:** Characterization freezes repeated-failure identity/count/escalation, reminder priority/cooldown/re-anchor/verification suppression, completion retry and terminal behavior, successful versus failed TODO updates, and exact result/recovery/ordinary/next-turn insertion order. No production defect was established. Focused, affected-package, race, full repository, and vet gates pass.
- [x] **T015** Implement and pass `RS-001..007` for lifecycle order, failures, cancellation, fatal/non-fatal persistence, serialization/isolation, exactly-once outcomes, and headless output; update trace rows. Dependencies: T010-T014. Covers: REQ-001, REQ-003, REQ-009, NFR-005, NFR-007; Plan: Shared run/session lifecycle. **Completed:** Characterization freezes successful and failed run ordering, finished run/error trace/summary persistence, model, tool, shell-descendant, and pending-approval cancellation, authoritative versus non-authoritative write behavior, same-runner serialization, independent-runner concurrency and state isolation, exactly-once outcome accounting and paths, and headless stdout behavior. The full gate independently proved and corrected a late shell-descendant side-effect gap in `d34b52a`; benchmark gate scheduling was stabilized in test-only commit `e94717d`. Focused and affected-package tests, race, full repository, and vet gates pass with a fixed test HOME.

**Checkpoint P0C**: Every shared row passes the corrected current implementation with one authoritative expectation set.

## Phase 0D: Profile, Adapter, and Control-Plane Characterization

- [x] **T020** Implement `PF-TUI-001..018` in app/TUI integration tests and update trace rows. Dependencies: T010-T015. Covers: REQ-002, REQ-010, REQ-014, NFR-005, NFR-007; Plan: TUIInteractive profile. **Completed:** all eighteen profile rows now trace exact current session selection/resume, serialized multi-run and FIFO queue behavior, new-session resets, future-run model/effort snapshots, root capabilities and restrictions, prompt commands, interactive questions/plans/permissions, prompt fragments and extraction, rewind/compaction, correlated observation, cancellation, and presentation isolation. Red characterization proved prompt-command preparation could continue into a model run after cancellation; independent TDD correction `63cee48` makes cancellation terminal before model dispatch. Profile, affected-package, race, vet, architecture, and complete repository gates pass with fixed test HOME.
- [x] **T021** Implement `UI-TUI-001..006` in `internal/tui/*_test.go` and `cmd/fox/main_test.go`, preserving exact terminal/input/render/overlay/process behavior; update trace rows. Dependencies: T020. Covers: REQ-008, REQ-010, REQ-014, NFR-005, NFR-007; Plan: TUI presentation. **Completed:** all six presentation groups trace exact launch/help/errors and initial prompt, editing/paste/history/completion/file mentions, built-ins and aliases/file commands/local shell/Autodev, transcript/Markdown/tools/sidebar/focus/selection/ANSI/HTML rendering, every blocking overlay, settings/status/notices/quit/error/interruption restoration. Red characterization proved all six overlay families overflowed a 12-row terminal; independent TDD correction `8ccdde1` adds a bounded overlay-only fallback without changing normal-size layouts. Package, race, vet, and complete repository gates pass with fixed test HOME.
- [x] **T022** Implement `PF-CLI-001..014` in app/CLI contract tests and update trace rows. Dependencies: T010-T015. Covers: REQ-002, REQ-010, REQ-014, NFR-005, NFR-007; Plan: CLIExec profile. **Completed:** all fourteen rows now trace exact immutable one-run configuration, CLI-only session selection, canonical tool and interaction ceilings, slash/skill/delegation behavior, prompt/context/state/compaction compatibility, extraction ownership, internal streaming without delta presentation, correlated outcomes/failures, and core presentation isolation. Shared runtime catalogs provide compaction, streaming, failure, cancellation, and exactly-once evidence. Characterization found no production defect. Focused, affected-package, race, vet, and complete repository gates pass; the full gate's unrelated Autodev signal-fixture race was synchronized independently in `837007b`.
- [x] **T023** Implement `UI-CLI-001..004` in `cmd/fox/main_test.go` and current CLI tests, asserting exact stdout/stderr/exit and output-before-drain behavior; update trace rows. Dependencies: T022. Covers: REQ-008, REQ-010, REQ-014, NFR-005, NFR-007; Plan: CLI presentation. **Completed:** all four rows now trace exact print routing, prompt/stdin acquisition and conflicts, provider/model/effort/thinking resolution and first-run error precedence, byte-exact stdout and isolated lifecycle stderr, output-before-drain ordering, initialization/nil/partial/runtime/extraction outcomes, and real process exit status 1. A package-private writer/factory seam exposes the current adapter for hermetic tests without changing public behavior. Focused, complete cmd/app, and race gates pass; full repository and vet gates are recorded in the implementation log.
- [x] **T024** Implement `PF-FEI-001..018` in `internal/feishu/*_test.go` using scripted runtime and messenger boundaries; update trace rows only after affected DV expectations are classified. Dependencies: T010-T015, T006. Covers: REQ-002, REQ-014, NFR-005, NFR-007, NFR-010; Plan: FeishuRemote profile. **Completed:** all eighteen rows now trace the immutable remote snapshot, typed source envelope, session directives/continuity/isolation, corrected FIFO/global scheduling and shutdown, exact tool and absent-interaction surfaces, Ask permission/evidence, shared one-level child runtime, context/compaction/model consistency, detached extraction, canonical observation, non-streaming execution, outcomes/cleanup, and core transport isolation. All ten previously proven Feishu defects use their independently corrected DV baselines. Characterization found no new production defect; the source-envelope formatter was extracted unchanged for exact testing. Focused, affected-package, and race gates pass; full repository and vet gates are recorded in the implementation log.
- [x] **T025** Implement `UI-FEI-001..007` in Feishu/cmd tests with authenticated local HTTP, duplicate, correlation, approval, delivery, and shutdown fixtures; update trace rows subject to DV conclusions. Dependencies: T024. Covers: REQ-008, REQ-009, REQ-014, NFR-005, NFR-007, NFR-010; Plan: Feishu transport. **Completed:** all seven rows now trace required environment ordering and exit, composition/listener/shutdown, private HTTP mux and timeouts, challenge and signed encryption, complete event translation including current malformed/unknown/non-text behavior, durable queue/idempotency, exact correlated outbound messages, Unicode/text bounds, SDK/API and typed delivery failures, and exactly-once approval callbacks. All tests are hermetic and use child processes, handlers, test storage, or fake SDK HTTP clients. Focused, complete cmd/Feishu/approval, and race gates pass; full repository and vet gates are recorded in the implementation log.
- [x] **T026** Implement `PF-AOP-001..019` in `internal/agentops/*_test.go` and update trace rows subject to AgentOps and shared Feishu DV conclusions. Dependencies: T010-T015, T006. Covers: REQ-002, REQ-014, NFR-005, NFR-007, NFR-010; Plan: AgentOpsTask profile. **Completed:** all nineteen rows now trace the immutable task snapshot, exact incident prompt, fresh-session isolation, corrected concurrency/shutdown/delivery behavior, exact capability and `log_search` boundaries, shared Ask approval and one-level child runtime, context/compaction/extraction, canonical outcomes/artifacts/cleanup, and core transport isolation. Existing shared and corrected DV contracts remain the sole authorities for permission, approval, ChildRun, engine, compaction, and process lifecycle. Characterization established no new production defect; focused and complete affected-package gates pass.
- [x] **T027** Implement `UI-AOP-001..006` in AgentOps/cmd tests for task mapping, queues, deduplication, exact messages, approvals, delivery failures, and shutdown; update trace rows subject to DV conclusions. Dependencies: T026. Covers: REQ-008, REQ-009, REQ-014, NFR-005, NFR-007, NFR-010; Plan: AgentOps transport. **Completed:** all six rows now trace six-variable process configuration and exit, settings composition and two capacity-64 queues, shared authenticated Gateway event validation, exact field-preserving bridge mapping/backpressure/closure, durable acceptance without the removed TTL Deduper, exact default/failure/artifact messages, bounded typed delivery failures, shared approvals, and coordinated shutdown. Direct tests reuse the already corrected Feishu Gateway/approval, AgentOps DV, and profile authorities rather than creating duplicate protocols. Characterization established no new production defect; complete affected packages pass.
- [x] **T028** Implement `PF-BEN-001..016` in `internal/benchmark/*_test.go` using fresh deterministic fixture workspaces and sessions; update trace rows subject to benchmark DV conclusions. Dependencies: T010-T015, T006. Covers: REQ-002, REQ-013, REQ-014, NFR-005, NFR-007, NFR-010; Plan: BenchmarkEval profile. **Completed:** all sixteen rows now trace the immutable corrected runtime spec, exact frozen case prompt, fresh serial repeat/session/workspace isolation, provider/model/tool and absent interaction surfaces, fixture-local context and direct skill fragments, session-only memory, selected-model compaction, shared policy/lifecycle, runtime-before-validation outcomes, complete provenance, cancellation/resource cleanup, and headless control-plane ownership. Existing corrected Benchmark DV and shared runtime catalogs remain the authorities for evaluation and lifecycle branches. Characterization found no new production defect; complete affected packages pass.
- [x] **T029** Implement `EV-BEN-001..011` in benchmark and `cmd/bench/main_test.go` for case input, validators, repeats, reports, provenance, failure precedence, and status; update trace rows subject to DV conclusions. Dependencies: T028. Covers: REQ-013, REQ-014, NFR-005, NFR-007, NFR-010; Plan: Benchmark control plane. **Completed:** all eleven rows now trace strict process flags and corrected positional rejection, exact case/YAML domains, rooted normalized fixtures, setup ordering and failures, bounded command/process-tree behavior, rooted file validation, ordered terminal aggregation, serial mixed repeats, byte-exact summaries, corrected JSON/provenance, and explicit load/execute/summary/write/exit precedence. Package-private FlagSet, command-dependency, and workspace-factory seams expose the unchanged current control plane to hermetic tests without creating another production path. Focused, affected-package, race, full repository, architecture, and vet gates pass.
- [x] **T030** Implement `PF-CHD-001..022` in `internal/subagent/*_test.go` and shared runtime tests for lineage, ceilings, cancellation, partial outcomes, and cleanup; update trace rows subject to child DV conclusions. Dependencies: T010-T015, T006. Covers: REQ-002, REQ-012, REQ-014, NFR-005, NFR-007, NFR-010; Plan: ChildRun profile. **Completed:** all twenty-two rows now trace the corrected flat current snapshot, frozen request/lineage/capability ceilings, fresh synchronous isolated execution, non-thinking/non-streaming provider use, exact tool/permission/prompt/skill/memory/compaction surfaces, one typed outcome, cleanup, and the legacy owner to migrate. D-CHD-007 through D-CHD-009 were independently proved and corrected before the profile baseline; existing DV and shared TL/CX/PL/RS contracts remain the stronger authorities for affected branches.
- [x] **T031** Implement `IA-CHD-001..006` for `delegate_task` and fork-skill schemas, normalization, assessment, invocation, and result adaptation; update trace rows. Dependencies: T030. Covers: REQ-011, REQ-012, NFR-005, NFR-007; Plan: Child invocation adapters. **Completed:** all six rows now trace the narrow delegate schema/domain, shared normalization and risk semantics, fail-closed coordination, exact one-call lineage and result adaptation, the processed/live fork snapshot, and headless single-path ownership. D-CHD-010 and D-CHD-011 were independently proved and corrected before the adapter baseline; no background/resume/team/deeper-tree protocol was added.
- [ ] **T032** Implement `PF-AUT-001..016` with a scripted core runner and deterministic item sessions; update trace rows subject to Autodev DV conclusions. Dependencies: T010-T015, T006. Covers: REQ-002, REQ-011, REQ-014, NFR-005, NFR-007, NFR-010; Plan: AutodevPipeline profile.
- [ ] **T033** Implement `CP-AUT-001..025` in `internal/autodev/*_test.go` using local repositories and fake command/GitHub boundaries; update trace rows subject to DV conclusions. Dependencies: T032. Covers: REQ-011, REQ-014, NFR-005, NFR-007, NFR-010; Plan: Autodev control plane.
- [ ] **T034** Implement `UI-AUT-001..006` in Autodev TUI/CLI adapter tests for exact reporting, cancellation, ownership, and process status; update trace rows. Dependencies: T032, T033. Covers: REQ-008, REQ-010, REQ-014, NFR-005, NFR-007; Plan: Autodev entry adapters.

## Phase 0E: Baseline Freeze

- [ ] **T046** Freeze `B00`: complete every trace row with test, fixture/outcome, command, passing result, and source commit; verify no skips or unresolved `DV-*`; run architecture baseline tests and `go test ./...`; record the baseline evidence. Dependencies: T001-T045. Covers: REQ-001..015, NFR-001..013; Plan: Phase 0 baseline gate.

**Checkpoint B00**: Production migration is now permitted. If this checkpoint is not complete, all following tasks remain blocked.

## Phase 1: Pure and Persisted Endpoints

- [ ] **T047 (`M01`)** Move deterministic renderer code from `internal/context` to new `internal/prompt`, retain a one-way compatibility facade, update import documentation/allowlist, and pass prompt goldens plus applicable context/profile contracts. Treat the move as mechanical unless a new renderer contract requires Red evidence. Dependencies: T046. Covers: REQ-007, NFR-001, NFR-011, NFR-013; Plan: Phase 1 M01.
- [ ] **T048 (`M02`)** TDD the persistence vocabulary and storage boundary in `internal/session`: `StoredSession`, `StoredRun`, `FileStore`, `ID`, `RunID`, `TranscriptEvent`, and `TranscriptLog`; retain one-way aliases/wrappers; pass all immutable session/compaction/checkpoint/rewind fixtures and update docs/allowlist. Dependencies: T047. Covers: REQ-006, NFR-001, NFR-009, NFR-011, NFR-013; Plan: Phase 1 M02.
- [ ] **T049 (`M03`)** TDD migration of all active session working-memory behavior to `internal/memory.Store`, remove production use of duplicate `session.WorkingMemory`, and pass memory/session/compaction/checkpoint/rewind contracts without changing fixtures. Dependencies: T048. Covers: REQ-006, NFR-001, NFR-011, NFR-013; Plan: Phase 1 M03.

## Phase 2: Target Engine

- [ ] **T050 (`M04`)** Write behavior-sensitive failing target-adapter tests, then add minimal engine `RunInput`, `RunContext`, `RunOutcome`, `ModelInvoker`, `ToolExecutor`, `Conversation`, `TurnPolicy`, and `Observer` contracts in `internal/engine`; prove tool-free completion, provider failure, and fact order while no production profile uses the target. Dependencies: T049. Covers: REQ-004, REQ-005, REQ-009, NFR-003, NFR-008, NFR-011; Plan: Phase 2 M04.
- [ ] **T051 (`M05`)** TDD the target model-turn loop for tool-free completion, thinking/action, streaming/fallback, limits, usage, and provider errors; pass `RT-001..007` and `ST-001..006` through both adapters. Dependencies: T050. Covers: REQ-004, REQ-005, NFR-007, NFR-008, NFR-011; Plan: Phase 2 M05.
- [ ] **T052 (`M06`)** TDD target `ToolExecutor` snapshots, correlated calls, parallel/exclusive scheduling, errors, large results, cancellation, and order; pass `TL-001..008` through both adapters. Dependencies: T051. Covers: REQ-005, REQ-014, NFR-007, NFR-008, NFR-011; Plan: Phase 2 M06.
- [ ] **T053 (`M07`)** TDD run-scoped recovery, reminder, completion, and TODO policies and remove target cross-run policy state; pass `PL-001..005` and related isolation scenarios through both adapters. Dependencies: T052. Covers: REQ-005, NFR-007, NFR-008, NFR-011; Plan: Phase 2 M07.
- [ ] **T054 (`M08`)** Complete the engine boundary by moving target context ownership, compaction, persistence, metrics, tracing, and presentation mapping outside engine; enforce only standard-library/schema imports; pass shared target contracts and decrease the allowlist. Dependencies: T053. Covers: REQ-004, REQ-005, REQ-009, NFR-002, NFR-003, NFR-012, NFR-013; Plan: Phase 2 M08.

## Phase 3: Runtime Lifecycle

- [ ] **T055 (`M09`)** TDD seven immutable `internal/runtime.Profile` resolvers, `RunSpec`, flat snapshots, validation, and non-relaxable ceiling intersection; pass every profile snapshot scenario. Dependencies: T054. Covers: REQ-002, REQ-014, NFR-003, NFR-011; Plan: Phase 3 M09.
- [ ] **T056 (`M10`)** TDD `RuntimeHarness` session creation/opening prerequisites, `AgentSession`, `RunScope`, and consumer-owned `SessionStore`; establish runtime as sole live recoverable-state owner and pass applicable `CX`, `RS`, and fixtures. Dependencies: T055. Covers: REQ-003, REQ-006, REQ-014, NFR-001, NFR-003, NFR-011; Plan: Phase 3 M10.
- [ ] **T057 (`M11`)** TDD `ContextController` collection, prompt injection, model-visible projection, compaction proposals, resume, and rewind with one `AgentSession` commit path; pass all applicable context/profile contracts. Dependencies: T056. Covers: REQ-003, REQ-006, REQ-007, NFR-001, NFR-007, NFR-011; Plan: Phase 3 M11.
- [ ] **T058 (`M12`)** TDD complete `RuntimeHarness` wiring for engine, model, tools, policies, observer, artifacts, persistence, compaction, memory, and telemetry; pass the full target shared-runtime suite without app/presentation imports. Dependencies: T057. Covers: REQ-001, REQ-003, REQ-009, REQ-015, NFR-003, NFR-007, NFR-011; Plan: Phase 3 M12.
- [ ] **T059 (`M13`)** TDD runtime `ChildRunner` with frozen parent snapshot, lineage, depth-one validation, capability intersection, inherited permission ceiling, cancellation, partial outcome, and cleanup; pass all `PF-CHD`, `IA-CHD`, and corrected DV expectations. Dependencies: T058. Covers: REQ-012, REQ-014, NFR-007, NFR-011; Plan: Phase 3 M13.

## Phase 4: Profile-Atomic Production Cutovers

- [ ] **T060 (`M14`)** Prove old/target benchmark parity, atomically migrate `internal/benchmark` and `cmd/bench` to `RuntimeHarness`, derive fidelity from resolved profile data, and remove independent benchmark engine assembly. Dependencies: T059. Covers: REQ-011, REQ-013, REQ-014, NFR-001, NFR-008, NFR-013; Plan: Phase 4 BenchmarkEval cutover.
- [ ] **T061 (`M15`)** Prove old/target child parity, adapt `delegate_task` and fork skills through consumer-owned `subagent.Runner` to runtime `ChildRunner`, and remove every legacy child-engine construction path. Dependencies: T060. Covers: REQ-011, REQ-012, REQ-014, NFR-001, NFR-003, NFR-013; Plan: Phase 4 ChildRun cutover.
- [ ] **T062 (`M16`)** TDD narrow `internal/app` command, DTO, notification, and interaction contracts; map runtime values without reverse imports; retain only the temporary facade needed by unmigrated user-entry profiles. Dependencies: T061. Covers: REQ-008, REQ-009, NFR-002, NFR-003, NFR-011, NFR-013; Plan: Phase 4 application boundary.
- [ ] **T063 (`M17`)** Prove full CLI parity, add `internal/cli.Run`, atomically migrate every CLI production path through app/runtime, preserve exact output and extraction drain ordering, and remove old CLI wiring. Dependencies: T062. Covers: REQ-001, REQ-008, REQ-010, REQ-014, NFR-001, NFR-007, NFR-013; Plan: Phase 4 CLIExec cutover.
- [ ] **T064 (`M18`)** Prove Autodev parity, migrate the production `CoreRunnerFactory` to runtime-backed sessions, keep control-plane responsibilities in Autodev, and remove `app.AgentRunner` and independent engine dependencies from Autodev production wiring. Dependencies: T063. Covers: REQ-011, REQ-014, NFR-001, NFR-007, NFR-013; Plan: Phase 4 AutodevPipeline cutover.
- [ ] **T065 (`M19`)** Prove Feishu parity, atomically migrate typed task execution to app/runtime while retaining transport/scheduling/approval ownership in Feishu, and remove old Feishu engine assembly. Dependencies: T064. Covers: REQ-001, REQ-008, REQ-014, NFR-001, NFR-007, NFR-013; Plan: Phase 4 FeishuRemote cutover.
- [ ] **T066 (`M20`)** Prove AgentOps parity, migrate task execution to app/runtime, reuse migrated Feishu transport/approval mechanisms, retain incident/log/control responsibilities, and remove old AgentOps engine assembly. Dependencies: T065. Covers: REQ-001, REQ-008, REQ-014, NFR-001, NFR-007, NFR-013; Plan: Phase 4 AgentOpsTask cutover.
- [ ] **T067 (`M21`)** TDD TUI-facing app capabilities and migrate run/session/model/effort/memory/compaction/checkpoint/rewind state away from concrete runtime/engine/session implementations; pass affected shared, profile, and UI scenarios. Dependencies: T066. Covers: REQ-008, REQ-010, REQ-014, NFR-001, NFR-011; Plan: Phase 4 TUI runtime state.
- [ ] **T068 (`M22`)** TDD application interaction ports and migrate TUI permissions, questions, Formal Plan review, notifications, cancellation, and queue coordination without Bubble Tea types crossing inward; pass all affected ordering and stale-event scenarios. Dependencies: T067. Covers: REQ-008, REQ-009, REQ-010, REQ-014, NFR-001, NFR-011; Plan: Phase 4 TUI interactions.
- [ ] **T069 (`M23`)** Prove complete TUI parity, atomically replace `app.RunTUI` with the single public `tui.Run`, restrict `cmd/fox` to composition/dispatch/startup, and remove the old TUI production path. Dependencies: T068. Covers: REQ-001, REQ-008, REQ-010, REQ-014, NFR-001, NFR-007, NFR-013; Plan: Phase 4 TUIInteractive cutover.

## Phase 5: Cleanup and Final Compatibility Gate

- [ ] **T070 (`M24`)** Delete `app.AgentRunner`, old application assembly, `app.RunCLI`, `app.RunTUI`, and all now-unused entry facades; prove all seven profiles use confirmed runtime/application boundaries and pass relevant catalogs. Dependencies: T069. Covers: REQ-001, REQ-008, REQ-010, NFR-002, NFR-013; Plan: Phase 5 M24.
- [ ] **T071 (`M25`)** Delete the old engine, old differential adapter, Reporter chain, old mutable configuration, and cross-run state; prove target engine/runtime own the full shared suite and engine imports remain restricted. Dependencies: T070. Covers: REQ-004, REQ-005, REQ-009, NFR-003, NFR-008, NFR-013; Plan: Phase 5 M25.
- [ ] **T072 (`M26`)** Delete the `internal/context` facade, temporary session aliases/wrappers, and duplicate memory owner; prove repository-wide symbol absence and unchanged fixture/context behavior. Dependencies: T071. Covers: REQ-006, REQ-007, NFR-001, NFR-009, NFR-013; Plan: Phase 5 M26.
- [ ] **T073 (`M27`)** Finalize `docs/package-dependencies.md` Mermaid and tables against implemented imports, empty `internal/architecturetest/allowlist.json`, run all architecture/scenario/fixture tests and `go test ./...`, verify one production path per profile and no generated worktree artifact, and publish final compatibility evidence. Dependencies: T072. Covers: all REQ-001..015 and NFR-001..013; Plan: Phase 5 M27 final gate.

## Dependencies and Execution Order

- `T001-T006` establish Phase 0 infrastructure.
- Defect gates `T040-T044` run next against the current production paths. A proven defect introduces an explicit stop and a separately approved defect task/commit before continuation.
- `T045` generates immutable compatibility fixtures once from the corrected source commit.
- Shared contracts `T010-T015` then execute sequentially against those fixtures and one scenario authority.
- Profile tasks `T020-T034` depend on the applicable shared contracts and execute sequentially on the current integration branch.
- `T046` depends on every Phase 0 task and is the only `B00` freeze point.
- `T047-T073` form the confirmed sequential `M01-M27` production chain. No production task may bypass a predecessor or `B00`.
- Profile cutovers and recoverable-state ownership boundaries are never combined. Adjacent mechanical work may be combined only under DEC-041 and only when its independent evidence remains intact.

## Required Verification Record

For each checked task, record in the task/implementation log:

- exact commands and results;
- Red failure reason when TDD applies;
- Green and post-refactor results;
- affected scenario IDs and trace rows;
- fixture hashes or expected-output references;
- architecture allowlist before/after when dependencies change;
- commit hash for every `Mxx` boundary and separate defect correction.

## Coverage

| Requirement / plan deliverable | Task references | Result |
|---|---|---|
| `REQ-001` Headless runtime | T015, T058, T063, T065-T066, T069-T070, T073 | Full |
| `REQ-002` Runtime Profiles | T020, T022, T024, T026, T028, T030, T032, T055 | Full |
| `REQ-003` Runtime ownership | T015, T056-T058 | Full |
| `REQ-004` Engine | T010-T011, T050-T054, T071 | Full |
| `REQ-005` Collaborators | T010-T014, T050-T054, T071 | Full |
| `REQ-006` Session/persistence | T013, T045, T048-T049, T056-T057, T072 | Full |
| `REQ-007` Prompt/context | T013, T047, T057, T072 | Full |
| `REQ-008` Application/adapters | T021, T023, T025, T027, T034, T062-T070 | Full |
| `REQ-009` Interactions/observation | T011, T015, T025, T027, T050, T054, T058, T062, T068, T071 | Full |
| `REQ-010` Presentation entries | T021-T023, T034, T063, T067-T070 | Full |
| `REQ-011` Control clients | T029, T031-T034, T060-T064 | Full |
| `REQ-012` Child depth/capability | T030-T031, T043, T059, T061 | Full |
| `REQ-013` Benchmark fidelity | T028-T029, T042, T060 | Full |
| `REQ-014` Profile bundles | T020-T034, T045, T055-T069 | Full |
| `REQ-015` Composition/injection | T002, T054, T058, T062, T073 | Full |
| `NFR-001` Behavior compatibility | T010-T046, T047-T073 | Full |
| `NFR-002` Focused boundaries | T002, T050-T054, T062, T070 | Full |
| `NFR-003` Dependency DAG | T002, T050, T054-T062, T071, T073 | Full |
| `NFR-004` Single repo/module | T046, T073 | Full |
| `NFR-005` Complete Phase 0 | T001, T005-T046 | Full |
| `NFR-006` Hermetic tests | T002-T006, T010-T046 | Full |
| `NFR-007` Catalog coverage | T001, T004, T010-T046, affected migration tasks | Full |
| `NFR-008` Old/target contracts | T004-T005, T050-T071 | Full |
| `NFR-009` Immutable fixtures | T003, T013, T045-T049, T072-T073 | Full |
| `NFR-010` Defect separation | T024-T045 | Full |
| `NFR-011` TDD and gates | T046-T073 plus Required Verification Record | Full |
| `NFR-012` Dependency docs/allowlist | T002, every dependency-changing migration task, T073 | Full |
| `NFR-013` Atomic migration | T046-T073 | Full |
| Shared Characterization Harness | T001, T003-T015 | Full |
| Runtime Profile and adapter catalogs | T020-T034 | Full |
| Residual defect gates and corrections | T040-T044, T074-T089 | Full |
| `B00` baseline | T045-T046 | Full |
| Engine component | T050-T054 | Full |
| Runtime component | T055-T059 | Full |
| Runtime control clients | T060-T061, T064 | Full |
| Application/presentation | T062-T063, T065-T070 | Full |
| Package documentation/enforcement | T002, T073 | Full |
| Mandatory cleanup | T070-T073 | Full |

## Unmapped Tasks

None.
