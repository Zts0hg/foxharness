### R-2026-0808-17255q-2: Run a defect-gate review loop over a large committed diff
- claim: The procedure for the isolated-reviewer defect-gate loop that reviews a branch against its baseline until a round admits zero findings.
- type: runbook
- scope/when: verifying a large refactor branch (hundreds of files) against a behavior-frozen baseline and origin/main
- steps: 1) resolve the review target with `.codexspec/scripts/review-context.sh --committed --base origin/main`; write manifest.json, evidence.diff, and fingerprint (sha256 of manifest ∥ diff) to `/tmp/review-rN/`; 2) dispatch 1–2 fresh isolated reviewer subagents per round (primary + area specialists), each invoking the review-code skill with the five mandatory stages, strict admission discipline (file:line + derivation + baseline evidence), neutral verification obligations, and read-only access; 3) verify every reported finding yourself with a direct probe, differential run, or minimal red test before accepting it; 4) fix accepted findings TDD-first and commit in defect-grouped commits (split by dependency order so every commit builds and passes); 5) re-resolve the target (new HEAD, new fingerprint) and dispatch the next round; 6) terminate when a round admits zero findings.
- failure-recovery: a reviewer killed by API/stream errors — resume it with SendMessage pointing at its last progress, or re-dispatch fresh; rate limits from parallel fleets — reduce to 1–2 reviewers and let them spawn their own bounded specialists; reviewer stalls — check the round's /tmp artifact directory for progress before re-dispatching; reviewer disagreement on a class — adjudicate per S-2026-0808-17255q-2 and encode the doctrine in the control's docs; a platform-only test failure after push — fix and push; the PR re-runs CI automatically.
- evidence.facts: rounds 15–26 of this loop over the decoupling branch verified and fixed ~45 defects across gate/runtime/telemetry/entry areas and converged to a PASS with zero admitted findings.
- evidence.state: confirmed at feature 2026-0808-17255q; HEAD 5d52983, PR #65 CI green. Still valid.
- provenance: distill @pr, 2026-08-31, derivation: inferred, confidence: high
- status: candidate
