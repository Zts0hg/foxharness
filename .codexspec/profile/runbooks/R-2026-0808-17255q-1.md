### R-2026-0808-17255q-1: Execute a behavior-preserving architecture migration
- claim: Use a characterization-first, defect-separated, bottom-up strangler workflow with profile-atomic cutovers and independently green commits.
- type: runbook
- scope/when: reorganizing shared production architecture without authorizing product behavior changes
- steps: 1) define every preserved user, module, entry, and persisted-data behavior; 2) assign stable scenario IDs and build complete hermetic black-box contracts, entry tests, deterministic collaborators, and immutable versioned fixtures before production movement; 3) prove every suspected pre-existing defect comprehensively, preserve unproven behavior, and correct each proven defect through its own Red-Green-Refactor requirement and commit before freezing B00; 4) document the target dependency DAG and start an exact immutable baseline plus decreasing architecture allowlist; 5) migrate bottom-up from pure/persisted endpoints to engine, runtime lifecycle, and then one complete Runtime Profile at a time, retaining only one-way temporary facades; 6) at every commit run focused contracts, affected profile catalogs, architecture checks, and fixture integrity, and remove resolved allowlist rows; 7) delete all compatibility facades and legacy production paths; 8) close with every scenario and fixture, empty allowlist, dependency-document parity, one production path per profile, artifact audit, `go test ./...`, race tests, and vet.
- failure-recovery: if characterization or trace coverage is incomplete, do not begin production movement; if a proof establishes a defect, stop that profile baseline until correction semantics and an independent Green commit are complete; if a migration gate fails, keep the previous production path authoritative and fix the current boundary without combining the next cutover; if sandbox policy alone blocks a hermetic test, distinguish the environmental denial from a behavioral Red and rerun the identical command in an authorized environment rather than weakening the test.
- evidence.facts: "重构开始之前需要补齐全部 characterization tests，不然重构前后没有一个可观测的标准判断重构前后的功能没有变化没有被破坏。"; "直接在当前集成分支开发，按模块形成清晰的独立 commit。不需要额外新创建分支和工作区。"; "采纳。并且增加一条需求，进行新代码实现时，严格遵守TDD (失败-通过-重构)的流程"
- evidence.state: executed end to end in feature `2026-0808-17255q`: defect commits preceded B00, M01-M27 followed the planned ownership and profile boundaries, the allowlist decreased from 68 to zero, 274 scenarios and 22 fixtures remained green, and final full/race/vet gates passed at `2d0cf12`. Still valid.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
