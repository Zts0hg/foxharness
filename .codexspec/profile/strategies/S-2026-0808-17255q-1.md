### S-2026-0808-17255q-1: Reproduce positional/desync bugs with displacement-amplifying inputs, not minimal ones
- claim: When a suspected index/offset desynchronization does not reproduce, amplify the structural displacement before doubting the finding.
- type: strategy
- scope/when: reproducing reported off-by-N, stale-index, or projection-shift defects where the corruption amount depends on input shape
- trigger: a reviewer-reported desync or corruption is plausible from code reading, but a first reproduction attempt with a small or minimal fixture passes.
- action: check whether the fixture accidentally keeps the displaced quantity at coincidentally identical positions (e.g. a rebuild that shrinks exactly one record); re-run with inputs that maximize the displacement — long histories, full-coverage transformations, boundary-crossing sizes — and assert on positions, not just membership.
- evidence.facts: the stale-index leak reproduced only after seeding five covered records and a compact state covering all of them (rebuild shrank the projection by five); with one record the stale marker still pointed at the notice and the test passed, masking the defect.
- evidence.state: confirmed at feature 2026-0808-17255q; commit 9ff9fc8. Still valid.
- provenance: distill @pr, 2026-08-31, derivation: inferred, confidence: high
- status: candidate
