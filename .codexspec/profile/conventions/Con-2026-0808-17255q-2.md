### Con-2026-0808-17255q-2: Compatibility fixtures are immutable, versioned, hashed, and copied before mutation
- claim: Persisted-data and output compatibility tests use committed versioned fixtures with source semantics and SHA-256 authority, verify them before use, and copy mutable inputs into test-owned roots instead of regenerating them with the implementation under test.
- type: convention
- scope/when: testing session data, rewind, compaction, checkpoints, memory, output formatting, benchmark reports, or other backward-compatible artifacts
- evidence.facts: "同意必须在重构之前补充完全各个测试，并且不能依赖真实外部依赖，应该使用固定持久化的可以在任何环境中反复运行的测试fxiture。"
- evidence.state: `testdata/characterization/v1` froze 22 payloads plus a manifest; integrity, unlisted-file rejection, copy isolation, and all persisted behavior tests passed through final commit `2d0cf12`. Still valid.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
