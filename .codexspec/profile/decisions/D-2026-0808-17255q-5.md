### D-2026-0808-17255q-5: Child execution is a single-level constrained runtime capability
- claim: Subagent delegation is model-facing tool adaptation over a runtime-owned child capability, limited to one nested level and one frozen lineage, model, permission, budget, and effective-tool snapshot; child code cannot recursively regain delegation through aliases or empty ceilings.
- type: decision
- scope/when: changing `delegate_task`, fork skills, subagent personas, child tools, or nested runtime execution
- evidence.facts: "subagent也不是用户层面的能力，而是 runtime的执行能力之一，并且需要跟你确认一个细节：现在subagent是使用tool delegate_task实现的是吗？所以本质上是一个工具，通过工具实现了 runtime的嵌套执行或者说递归执行？"; "保持一层"
- evidence.state: D-CHD corrections froze identity and capabilities before B00; M13 and M15 established `subagent.Runner -> childruntime.Runner -> runtime.ChildRunner`, enforced depth one, and preserved typed correlated outcomes. Still valid at `2d0cf12`.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
