### D-2026-0808-17255q-1: Keep one Go module with an enforced package DAG
- claim: Fox remains one repository and one Go module, with presentation adapters depending on `internal/app`, application capabilities depending on `internal/runtime`, runtime depending on the infrastructure-independent `internal/engine`, and concrete mechanisms injected only at composition roots through consumer-owned ports.
- type: decision
- scope/when: adding packages, entry points, callbacks, dependency injection, or cross-package capabilities
- evidence.facts: "采用\"单仓库、单 Go module、强 package 边界。"; "确认，不创建统一的 internal/infrastructure package，避免重蹈覆辙让一个package拥有过多过大的职责，导致违背高内聚低耦合的设计原则。"
- evidence.state: implemented by M01-M26 and documented in `docs/package-dependencies.md`; architecture tests report zero forbidden production edges and `RuntimeHarness` is the sole engine assembler at `2d0cf12`. Still valid.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
