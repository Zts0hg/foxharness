### D-2026-0808-17255q-6: Engine is a pure turn state machine with run-scoped state
- claim: `internal/engine` coordinates turn transitions through consumer-owned model, tool, conversation, policy, and observer ports, retains only immutable collaborators, and owns no provider transport, concrete tool dispatch, compaction, persistence, telemetry, presentation, or mutable cross-run state.
- type: decision
- scope/when: changing the agent loop, model/tool collaboration, turn policy, conversation projection, or engine dependencies
- evidence.facts: "同意将顶层 engine 收缩为纯粹的 turn state machine，另外需要检查你建议的划分是否合理且不存在冗余，以保持可维护性可扩展性和可读性（符合我们的重构目的）。"
- evidence.state: M04-M08 established the target engine and run-scoped collaborator lifetimes; M25 deleted the legacy engine, Reporter chain, concrete infrastructure imports, and cross-run mutable state. Architecture tests restrict production engine imports to the standard library and `internal/schema` at `2d0cf12`. Still valid.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
