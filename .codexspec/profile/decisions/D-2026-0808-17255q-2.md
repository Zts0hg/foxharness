### D-2026-0808-17255q-2: Separate live recoverable state from persistence and observation
- claim: `runtime.AgentSession` is the sole coordinator of live recoverable state, `internal/session` owns persisted records and file mechanics only, `memory.Store` owns working-memory files, and journals, metrics, traces, and reporters remain non-authoritative observations that cannot decide recovery or terminal outcomes.
- type: decision
- scope/when: changing session lifecycle, rewind, compaction, memory, transcript, metrics, tracing, or run completion
- evidence.facts: "保留 internal/session，通过类型和职责边界区分 persistence session records 与 runtime.AgentSession，不进行 session → sessionstore 路径改名。"; "确认： “保持行为不变”包括 CLI/TUI 交互、权限语义、工具集合、session/rewind/compaction、远程入口和 benchmark 行为。"
- evidence.state: M10-M11 established the runtime commit authority; M25-M26 removed legacy lifecycle wrappers, duplicate state owners, and the final `session -> memory` edge while persisted compatibility fixtures remained byte-stable. Still valid at `2d0cf12`.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
