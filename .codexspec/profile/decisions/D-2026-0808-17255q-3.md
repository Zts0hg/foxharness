### D-2026-0808-17255q-3: Runtime owns complete-context lifecycle; prompt only renders fragments
- claim: `internal/prompt` is a deterministic side-effect-free fragment renderer, while runtime owns discovery, injection timing, collaboration and capability selection, memory reads, compaction coordination, and complete model-context lifecycle.
- type: decision
- scope/when: changing system prompts, project instructions, skills, collaboration mode, memory injection, automemory, or context preparation
- evidence.facts: "确认 internal/context → internal/prompt，同时明确：它是纯 prompt fragment renderer，不是完整的 context owner；完整上下文的生命周期和注入决策属于 runtime。"
- evidence.state: M01 introduced the pure renderer; M11 established runtime context authority; M26 deleted `internal/context` and moved production collection to runtime `PromptCollector`. Prompt imports only the standard library at `2d0cf12`. Still valid.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
