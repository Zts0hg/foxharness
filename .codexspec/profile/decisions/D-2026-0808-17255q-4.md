### D-2026-0808-17255q-4: Use one headless runtime with explicit immutable profiles
- claim: Every production entry resolves an explicit immutable Runtime Profile and per-run narrowing input; UI and remote entries use application presentation adapters, while Benchmark and Autodev are privileged runtime control clients that exercise the same real harness without passing through the user-interaction layer.
- type: decision
- scope/when: adding or changing an entry point, execution mode, runtime preset, benchmark, or automation client
- evidence.facts: "采用推荐方案，将 TUI 仅拆分为 Fox application 的 presentation adapter，不在本次重构中抽取通用 pi-tui 式终端 UI 库"; "benchmark是服务和测试核心agent能力的，不属于用户交互层面的内容，所以benchmark应该是作为runtime harness的一部分，用于评估与反馈，可以直接控制核心模块。"
- evidence.state: M09 froze seven profile contracts; M14-M23 cut them over atomically; final architecture has one production path for TUIInteractive, CLIExec, FeishuRemote, AgentOpsTask, BenchmarkEval, ChildRun, and AutodevPipeline. Still valid at `2d0cf12`.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: explicit, confidence: high
- status: vetted
