### P-2026-0808-17255q-3: Nil and empty capability sets must not share semantics
- claim: Capability APIs must preserve the distinction between unspecified/unrestricted and explicitly empty/deny-all values through copies, intersections, aliases, and adapters.
- type: pitfall
- scope/when: narrowing tools, permissions, profiles, child capabilities, or optional policy lists in Go
- root-cause: nil and zero-length slices are easily collapsed by constructors, append patterns, JSON/YAML handling, or helper normalization; if nil means unrestricted, an explicit empty intersection can accidentally restore every capability.
- workaround: define nil and empty semantics explicitly, preserve them in cloning and intersection code, use stable canonical ordering and alias expansion from the same snapshot, and test explicit deny-all through both advertisement and execution paths.
- lesson: absence and an empty decision are different security states; represent and test them as different values.
- evidence.facts: "保持一层"; "需要确保所有的用户交互行为和模块功能行为都保持不变"
- evidence.state: ChildRun D-CHD-005 and D-CHD-003 reproduced empty intersections restoring the full registry and then verified deny-all, aliases, immutable schemas, permission metadata, prompt claims, and execution from one snapshot. Still valid at `2d0cf12`.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: inferred, confidence: high
- status: candidate
