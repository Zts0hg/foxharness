### P-2026-0808-17255q-4: A cancelled operation context cannot complete terminal cleanup reliably
- claim: Terminal cleanup, failure delivery, and read-only reconciliation after cancellation must use a fresh bounded context and must never authorize new work.
- type: pitfall
- scope/when: cancellation or timeout is followed by cleanup, process reaping, workspace removal, terminal notification, drain, or ground-truth verification
- root-cause: reusing the expired operation context causes terminal work to fail immediately, hiding cleanup or delivery failure and leaving resources or accepted work unaccounted for; detaching without a bound can instead hang shutdown or accidentally continue workflow execution.
- workaround: stop admission first, preserve the original cancellation classification, derive a fresh context with a fixed deadline only for terminal cleanup or read-only reconciliation, join its evidence into the original outcome, and prohibit retries, state advancement, or new side effects from that context.
- lesson: operation lifetime and terminalization lifetime are distinct, but terminalization remains bounded and non-progressing.
- evidence.facts: "每个模块 commit 都应通过相关 package 测试，最终 PR 必须通过完整测试和兼容性验证。"; "确认"
- evidence.state: Feishu panic/cancellation delivery, Benchmark workspace cleanup, ChildRun process cleanup, and Autodev drain/close/reconciliation each required this distinction and passed focused cancellation, race, and final full-suite tests. Still valid at `2d0cf12`.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: inferred, confidence: high
- status: candidate
