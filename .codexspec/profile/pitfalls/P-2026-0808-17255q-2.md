### P-2026-0808-17255q-2: Lexical path validation does not confine filesystem access
- claim: Security-sensitive file access must be rooted at an opened authority and validate the object actually opened; cleaned strings and prefix checks do not prevent symlink or replacement escapes.
- type: pitfall
- scope/when: reading or writing fixtures, logs, worktrees, feature artifacts, validators, or persisted paths beneath a configured root
- root-cause: traversal checks operate on path text, while symlink resolution and time-of-check/time-of-use replacement operate on filesystem objects; a lexically valid child can therefore resolve outside the intended root or to a non-regular target.
- workaround: validate the relative logical form, open the trusted root with `os.Root`, traverse through rooted operations, reject symlinks where required, inspect the opened handle as a regular file, and perform durable writes through a rooted temporary regular file plus rename.
- lesson: confinement requires object-capability-style rooted access and post-open validation, not string normalization alone.
- evidence.facts: "不要抽查，要全面检查核对"; "确认"
- evidence.state: AgentOps D-AOP-006, Benchmark D-BEN-004, and Autodev D-AUT-005 independently proved outside-root symlink or traversal access; rooted regular-file implementations passed focused security, race, and complete repository tests. Still valid at `2d0cf12`.
- provenance: distill of full feature branch `2026-0808-17255q-agent-architecture-decoupling`, 2026-08-21, derivation: inferred, confidence: high
- status: candidate
